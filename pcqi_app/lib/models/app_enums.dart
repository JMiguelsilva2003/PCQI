enum RequestTypeAdminMachineControl { none, pause, resume, ejectManual }

enum RequestStatusAdminMachineControl { none, sucess, fail, userNotAdmin }

enum SearchBarFilters { noFilter, bySector, byMachine }
