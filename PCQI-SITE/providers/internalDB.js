import {
  get,
  set,
  getMany,
  setMany,
  update,
  del,
  clear,
  keys,
  values,
  entries,
  createStore,
} from './indexedDB';

(function init(){
  // planejar pos processamento de dados
})();